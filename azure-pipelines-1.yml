# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool: Tx_HYD_LAP_118
steps:

- task: UiPathTest@2
  inputs:
    testTarget: 'TestSet'
    orchestratorConnection: 'Orchestrator'
    testSet: 'Tx_HyperAutomationProject'
    folderName: 'Testing21TxUiPathFolder'

